type IconProps = {
    sx?: string
    className?: string
    style?: string
}

const Icon = ({
    sx,
    className,
    style,
}: IconProps) => {
    const renderGraphic = () => {
        
    };
return ({ renderGraphic })

}